name: Build, Test and Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Docker
        uses: docker/setup-buildx-action@v3
        with:
          install-compose: true

      - name: Build with Docker Compose
        run: docker compose build

      - name: Test services start correctly
        env:
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          PGADMIN_PASSWORD: ${{ secrets.PGADMIN_PASSWORD }}
        run: |
          # Запускаем контейнеры
          docker compose up -d
          
          # Ждем запуска
          sleep 30
          
          # Проверяем статус
          docker compose ps
          
          # Проверяем что оба сервиса запущены
          RUNNING_COUNT=$(docker compose ps | grep "Up" | wc -l)
          echo "Running containers: $RUNNING_COUNT"
          
          if [ $RUNNING_COUNT -lt 2 ]; then
            echo "❌ Not all services are running"
            docker compose logs
            exit 1
          fi
          
          # Проверяем health endpoints
          echo "Testing backend health..."
          curl -f http://localhost:15378/health || echo "⚠️ Backend health check failed"
          
          echo "Testing frontend..."
          curl -f http://localhost:14000 || echo "⚠️ Frontend check failed"
          
          docker compose down

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server using SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || '22' }}
          script: |
            echo "🚀 Starting deployment..."
            cd ${{ secrets.SERVER_PATH }}
            
            echo "Pulling latest code..."
            git pull origin main
            
            echo "Stopping old containers..."
            docker-compose down
            
            echo "Building new containers..."
            docker-compose build --no-cache
            
            echo "Starting containers..."
            POSTGRES_PASSWORD="${{ secrets.POSTGRES_PASSWORD }}" \
            PGADMIN_PASSWORD="${{ secrets.PGADMIN_PASSWORD }}" \
            docker-compose up -d
            
            echo "Cleaning up..."
            docker system prune -f
            
            echo "✅ Deployment completed!"
            docker-compose ps